import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignSelf: 'stretch',
    marginBottom: 10,
    alignItems: 'center',
  },

  row: {
    flexDirection: 'row',
    flexWrap: 'nowrap',
  },
});
